{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "transpile",
            "type": "node",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "program": "${workspaceRoot}/transpile.js"
        }
    ]
}